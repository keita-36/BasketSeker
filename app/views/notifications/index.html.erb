<h3 class="text-2xl font-black text-center text-3xl">通知</h3>
<br>
<% if @notifications.exists? %>
    <div class="bg-white shadow overflow-hidden sm:rounded-md text-left mx-auto max-w-lg mb-40">
        <div class="text-left">
            <%= render @notifications %>
        </div>
    </div>
<% else %>
    <p class="text-gray-500 ">通知はありません</p>
<% end %> 
